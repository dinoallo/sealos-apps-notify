FROM scratch

ARG TARGETARCH

COPY registry_${TARGETARCH} registry
COPY charts charts
COPY sealos-notify-entrypoint.sh sealos-notify-entrypoint.sh

ENV RELEASE_NAME=sealos-notify
ENV RELEASE_NAMESPACE=notify-system

CMD ["bash sealos-notify-entrypoint.sh"]
